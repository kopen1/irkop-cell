# Secrets & credentials
.env
.env.*
!.env.example
# Di-commit: hanya berisi VITE_API_BASE (bukan rahasia), dibutuhkan saat
# Cloudflare Pages build agar frontend memanggil /api dengan benar.
!frontend/.env.production
.dev.vars
token.md
token.md.bak
*.pem
*.key

# Dependencies & build artifacts
node_modules/
dist/
.wrangler/

# Logs
*.log

# Dev scripts (refer to /tmp paths, not portable)
start-local.sh
start-preview.sh

# Dev docs & benchmarks (not essential for app)
backend/bench/
backend/docs/
frontend/docs/

# Nested repo artefact (source sudah di-flatten ke root)
Revisi/
